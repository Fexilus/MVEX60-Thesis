% Custom definitions

% amsthm definitions
% TODO: Decide on a style. Right now these are copied from amsthm's docs.
\theoremstyle{plain}
\newtheorem{thm}{Theorem}[section]
\newtheorem{lem}[thm]{Lemma}
\newtheorem{prop}[thm]{Proposition}
\newtheorem*{cor}{Corollary}

\theoremstyle{definition}
\newtheorem{defn}{Definition}[chapter]
\newtheorem{exmp}{Example}[section]
\newtheorem{xca}[exmp]{Exercise}

\theoremstyle{remark}
\newtheorem*{rem}{Remark}
\newtheorem*{note}{Note}
\newtheorem{case}{Case}

% Sets
\newcommand{\reals}{\mathbb{R}}
\newcommand{\cont}[2]
{
  \ensuremath{
    \mathcal{C}
    \if\relax\detokenize{#1}\relax\else^{#1}\fi
    \if\relax\detokenize{#2}\relax\else\left(#2\right)\fi
  }
}

% TODO: Derivatives and elongations as style not content

% Math table-likes
\newcommand\FlLabel[1]{% Label that is affects the maximal spreading of flalign.
  \,\label{#1}% TODO: Sometimes overflows last label
}

% TODO: Create system to reference ODEs
% Subequation references
\crefname{subequation}{eqs.}{eqs.}
% FIXME: Temp fix for reference in mathmode
\newcommand{\ghostref}[1]{}
\autonum@generatePatchedReference{ghostref}

% Notation
%\RequirePackage{xspace}
% Fixed values
\newcommand{\fixed}[1]{#1^*}
% Total derivative
\newcommand{\tD}[1]{D_{#1}\,}

% Jet space commands
\newcommand*{\prolong}[2]{\ensuremath{\underset{#2}{#1}}}
\newcommand*{\prolongpart}[2]{\ensuremath{{#1_{(#2)}}}}
\newcommand*{\variety}{\ensuremath{\mathcal{S}}}
